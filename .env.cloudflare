# Cloudflare Pages + Workers 環境配置
NODE_ENV=production
VITE_API_BASE_URL=https://your-domain.pages.dev/api
VITE_APP_TITLE=蘿蔔糕訂購系統後台 (雲端版)
VITE_ENVIRONMENT=cloudflare-pages

# Cloudflare Workers 配置
WORKERS_API_ENABLED=true
WORKERS_KV_NAMESPACE=orders_cache
ENABLE_DEBUG_LOGS=false

# Google Sheets 配置 (使用 Cloudflare 環境變數)
GOOGLE_SHEETS_ID=10MMALrfBonchPGjb-ps6Knw7MV6lllrrKRCTeafCIuo
# 注意：service account key 在 Cloudflare 中應使用環境變數存儲